<UserControl x:Class="TourPlanner.View.Subviews.Maincontent.MaincontentTourlogs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.View.Subviews.Maincontent"
                          xmlns:maincontent="clr-namespace:TourPlanner.View.Subviews.Maincontent"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <GroupBox Header="Tour Logs" Margin="5" Width="auto">
            <DataGrid ItemsSource="{Binding TourLogs}" AutoGenerateColumns="False"
                                CanUserAddRows="False" ColumnWidth="*" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*"/>
                    <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="*"/>
                    <DataGridTextColumn Header="Distance" Binding="{Binding Distance}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    
</UserControl>
