<UserControl x:Class="TourPlanner.View.Subviews.Maincontent.MaincontentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.View.Subviews.Maincontent"
             xmlns:maincontent="clr-namespace:TourPlanner.View.Subviews.Maincontent"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Tabs -->
            <RowDefinition Height="2*"/>
            <!-- Route Map -->
            <RowDefinition Height="1*"/>
            <!-- Tour Logs -->
        </Grid.RowDefinitions>

        <!-- Tabs -->
        <maincontent:MaincontentTabs Grid.Row="0"> </maincontent:MaincontentTabs>

        <!-- Map -->
        <maincontent:MaincontentMap Grid.Row="1">
        </maincontent:MaincontentMap>

        <!-- Tour Logs -->


        <maincontent:MaincontentTourlogs  Grid.Row="2" >
            
        </maincontent:MaincontentTourlogs>
        
    </Grid>
    
    
    
</UserControl>
