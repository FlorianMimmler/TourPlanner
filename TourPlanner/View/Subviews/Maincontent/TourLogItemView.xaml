<UserControl x:Class="PresentationLayer.View.Subviews.Maincontent.TourLogItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.View.Subviews.Maincontent"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="5" Padding="5" Background="{DynamicResource WindowBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding Date}" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontSize="14"/>
            <TextBlock Text="{Binding Distance}" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" FontSize="14"/>
            <TextBlock Text="{Binding Duration}" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" FontSize="14"/>
            <TextBlock Text="{Binding Difficulty, StringFormat=' {0}/10'}" Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" FontSize="14"/>
            <TextBlock Text="{Binding Rating, StringFormat=' {0}/10'}" Grid.Row="0" Grid.Column="4" VerticalAlignment="Center" FontSize="14"/>

            <Button Content="Edit" Grid.Row="0" Grid.Column="5" FontSize="14"
                    Command="{Binding OpenModifyTourLogCommand}"
                    Foreground="{DynamicResource ControlDarkBrush}"
                    Style="{StaticResource BaseButtonStyle}"/>

            <TextBlock Text="{Binding Comment, StringFormat='Comment: {0}'}" Grid.Row="1" Grid.ColumnSpan="6" VerticalAlignment="Center" FontSize="14" Margin="0,5,0,0"/>
        </Grid>
    </Border>
</UserControl>
