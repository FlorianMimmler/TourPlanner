<Window x:Class="TourPlanner.View.CreateTourLogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner.View"
        mc:Ignorable="d"
        xmlns:viewmodel="clr-namespace:TourPlanner.BusinessLayer.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewmodel:CreateTourViewModel}"
        SizeToContent="Height"
        Title="CreateTourLogView" Height="450" Width="800">
    
    
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="20" Text="Create Tour Log:"></TextBlock>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Grid.Column="0" FontSize="16">Select Tour:</TextBlock>
            <ComboBox SelectedItem="{Binding SelectedTransportType, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Tours}"  Grid.Column="0" FontSize="14" Padding="2"></ComboBox>
        </StackPanel>




    </Grid>
    
    
    
    
</Window>
