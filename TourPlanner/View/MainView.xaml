<Window x:Class="TourPlanner.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner.View" xmlns:sys="clr-namespace:System;assembly=netstandard"
        xmlns:sidebar="clr-namespace:TourPlanner.View.Subviews.Sidebar" 
        xmlns:menu="clr-namespace:TourPlanner.View.Subviews.Menu" 
        mc:Ignorable="d"
        Title="Tour Planner" Height="450" Width="800">

    <Grid>
        <!-- Top Menu -->
        <DockPanel LastChildFill="True">
            
            <menu:MenuView DockPanel.Dock="Top"></menu:MenuView>

            <!-- Main Layout -->
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <!-- Sidebar -->
                    <ColumnDefinition Width="200"/>
                    <!-- Spacer Column for the Border -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Main Content -->
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Tour List (Sidebar) -->
                <sidebar:SidebarView Grid.Column="0" Margin="5 0 5 0"></sidebar:SidebarView>
                <!-- <Sidebar:SidebarView  Grid.Column="0" Margin="0 0 5 0"></Sidebar:SidebarView> -->

                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" />
                
                <!-- Main Content -->
                <Grid Grid.Column="2" Margin="5 0 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Tabs -->
                        <RowDefinition Height="2*"/>
                        <!-- Route Map -->
                        <RowDefinition Height="1*"/>
                        <!-- Tour Logs -->
                    </Grid.RowDefinitions>

                    <!-- Tabs -->
                    <TabControl Grid.Row="0">
                        <TabItem Header="General"/>
                        <TabItem Header="Route"/>
                        <TabItem Header="Misc"/>
                    </TabControl>

                    <!-- Map -->
                    <Border Grid.Row="1" BorderBrush="Black" BorderThickness="1" Margin="5">
                    </Border>

                    <!-- Tour Logs -->
                    <GroupBox Grid.Row="2" Width="auto" Header="Tour Logs" Margin="5">
                        <DataGrid ItemsSource="{Binding TourLogs}" AutoGenerateColumns="False"
                                CanUserAddRows="False" ColumnWidth="*" >
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*"/>
                                <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="*"/>
                                <DataGridTextColumn Header="Distance" Binding="{Binding Distance}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>
                </Grid>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
