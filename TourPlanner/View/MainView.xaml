<Window x:Class="TourPlanner.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner.View" xmlns:sys="clr-namespace:System;assembly=netstandard"
        xmlns:sidebar="clr-namespace:TourPlanner.View.Subviews.Sidebar" 
        xmlns:menu="clr-namespace:TourPlanner.View.Subviews.Menu" 
        xmlns:maincontent="clr-namespace:TourPlanner.View.Subviews.Maincontent" 
        
        mc:Ignorable="d"
        Title="Tour Planner" Height="450" Width="800">

    <Grid>
        <DockPanel LastChildFill="True">
            
            <!-- Top Menu -->
            <menu:MenuView DockPanel.Dock="Top"></menu:MenuView>

            <!-- Main Layout -->
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <!-- Sidebar -->
                    <ColumnDefinition Width="200"/>
                    <!-- Spacer Column for the Border -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Main Content -->
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <sidebar:SidebarView x:Name="sidebarView" Grid.Column="0" Margin="5 0 5 0"></sidebar:SidebarView>

                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" />
                
                <!-- Main Content -->
                <maincontent:MaincontentView Grid.Column="2" Margin="5 0 5 0" Loaded="MaincontentView_Loaded"></maincontent:MaincontentView>
            
            
            </Grid>
        </DockPanel>
    </Grid>
</Window>
